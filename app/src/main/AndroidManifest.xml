<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="Teste3"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Teste3"
        tools:targetApi="31">

        <!-- Login — tela inicial do app -->
        <activity
            android:name=".login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".login.CadastrarContaActivity"
            android:exported="false" />

        <activity
            android:name=".login.RecuperarSenhaActivity"
            android:exported="false" />

        <activity
            android:name=".login.ChatbotActivity"
            android:exported="false" />

        <!-- Home -->
        <activity
            android:name=".home_aluno.HomeActivity"
            android:exported="false" />

        <!-- Detalhes -->
        <activity
            android:name=".detalhe_livro.BookDetailActivity"
            android:exported="false" />

        <!-- Aluguel -->
        <activity
            android:name=".aluguel_livros.AluguelActivity"
            android:exported="false" />

        <!-- MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

    </application>

</manifest>